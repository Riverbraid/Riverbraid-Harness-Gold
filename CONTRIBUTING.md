# Contributing to Riverbraid
1. Coherence over Speed.
2. Follow the ASCII Floor policy.
3. Every PR must include a `verify.mjs` execution result.
4. No nondeterminism (Date.now(), Math.random()) allowed in core logic.
