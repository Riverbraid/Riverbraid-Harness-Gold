{
  "name": "riverbraid-harness-gold",
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "preflight": "node -e \"console.log(\"Preflight: Substrate Mapping Active\")\"",
    "entropy": "node scripts/entropy-check.mjs",
    "clean": "node scripts/clean.js",
    "test": "npm run clean && npm run entropy && node src/index.js"
  }
}